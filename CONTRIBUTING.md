# 协作约定

## 提交原则

- `main` 分支尽量保持可运行。
- 一个提交尽量只处理一个明确问题。
- 不提交本地数据库、真实用户图片、大型数据集和训练权重。
- 不提交无来源的农业阈值、药剂剂量或处方。
- 新功能尽可能同步增加测试。
- 不为了增加代码量加入无意义代码。

## Commit 前缀

建议使用：

- `feat:` 新功能
- `fix:` 缺陷修复
- `test:` 测试
- `docs:` 文档
- `refactor:` 重构
- `chore:` 工程维护

## 协作流程

较大功能建议：

1. 从 `main` 创建功能分支；
2. 完成功能与测试；
3. Push 到 GitHub；
4. 创建 Pull Request；
5. 同事 Review；
6. 合并到 `main`。
